<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kopo.poly.persistance.mapper.IUserInfoMapper">

    <select id="getUserIdExists" parameterType="UserInfoDTO" resultType="UserInfoDTO">
        SELECT DECODE(COUNT(1), 0, 'N', 'Y') AS EXISTS_YN
        FROM USER_INFO
        WHERE USER_ID = #{userId}
    </select>

<!--    resultType, resultMap, resultSet, resultSetType, resultOrdered-->
    <select id="getEmailExists" parameterType="UserInfoDTO" resultType="UserInfoDTO">
        SELECT DECODE(COUNT(1), 0, 'N', 'Y') AS EXISTS_YN
        FROM USER_INFO
        WHERE EMAIL = #{email}
    </select>

    <insert id="insertUserInfo" parameterType="UserInfoDTO">
        INSERT INTO USER_INFO
        (USER_ID,
         USER_NAME,
         PASSWORD,
         EMAIL,
         ADDR1,
         ADDR2,
         REG_ID,
         REG_DT,
         CHG_ID,
         CHG_DT)
        VALUES (#{userId},
                #{userName},
                #{password},
                #{email},
                #{addr1},
                #{addr2},
                #{userId},
                SYSDATE,
                #{userId},
                SYSDATE
                )
    </insert>

    <select id="getUserInfoList" resultType="UserInfoDTO">
        SELECT * FROM USER_INFO
        ORDER BY REG_DT
    </select>

    <select id="getUserInfo" resultType="UserInfoDTO" parameterType="UserInfoDTO">
        SELECT * FROM USER_INFO
        WHERE USER_ID = #{userId}
    </select>

    <select id="getLoginInfo" resultType="UserInfoDTO" parameterType="UserInfoDTO">
        SELECT USER_ID, USER_NAME, EMAIL
        FROM USER_INFO

        WHERE USER_ID = #{userId}
        AND PASSWORD = #{password}
    </select>
</mapper>